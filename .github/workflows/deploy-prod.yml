# This is a basic workflow to help you get started with Actions

name: Prod Deploy

on:
  push:
    branches:
      main

env:
  PROJECT_DIR: /home/rasulkireev/levreview
  DO_PASSWORD: ${{ secrets.DO_PASSWORD }}

jobs:
  prepare:
    name: Prepare Deployment
    runs-on: ubuntu-latest
    steps:
      - name: Pull the Latest Commit
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.DO_HOST }}
          username: ${{ secrets.DO_USERNAME }}
          key: ${{ secrets.DO_SSHKEY }}
          script_stop: true
          envs: PROJECT_DIR
          script: |
            cd $PROJECT_DIR
            git fetch --all
            git reset --hard origin/main

      - name: Install Conf Files
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.DO_HOST }}
          username: ${{ secrets.DO_USERNAME }}
          key: ${{ secrets.DO_SSHKEY }}
          script_stop: true
          envs: PROJECT_DIR,DO_PASSWORD
          script: |
            echo "$DO_PASSWORD" | sudo -S cp $PROJECT_DIR/deployment/levreview.gunicorn.socket /etc/systemd/system/
            echo "$DO_PASSWORD" | sudo -S cp $PROJECT_DIR/deployment/levreview.gunicorn.service /etc/systemd/system/

            echo "$DO_PASSWORD" | sudo -S systemctl start levreview.gunicorn.socket
            echo "$DO_PASSWORD" | sudo -S systemctl enable levreview.gunicorn.socket
            echo "$DO_PASSWORD" | sudo -S systemctl status levreview.gunicorn.socket

            file /run/levreview.gunicorn.sock
            echo "$DO_PASSWORD" | sudo -S journalctl -u levreview.gunicorn.socket
            echo "$DO_PASSWORD" | sudo -S systemctl status gunicorn

            echo "$DO_PASSWORD" | sudo -S cp $PROJECT_DIR/deployment/levreview.nginx.conf /etc/nginx/sites-available/
            echo "$DO_PASSWORD" | sudo -S ln -s /etc/nginx/sites-available/levreview.nginx.conf /etc/nginx/sites-enabled
            echo "$DO_PASSWORD" | sudo -S nginx -t
            echo "$DO_PASSWORD" | sudo -S systemctl restart nginx
